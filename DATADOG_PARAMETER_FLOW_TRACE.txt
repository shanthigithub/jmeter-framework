COMPLETE DATADOG PARAMETER FLOW - MANUAL VERIFICATION GUIDE
================================================================

Step 1: GitHub Actions Workflow Input
--------------------------------------
FILE: .github/workflows/run-test.yml
LINE: 110-122

User selects: "yes" or "no"
Code converts to: "true" or "false"

```yaml
ENABLE_DATADOG_INPUT="${{ github.event.inputs.enable_datadog_metrics }}"

# Convert yes/no to true/false for container environment
if [ "$ENABLE_DATADOG_INPUT" = "yes" ]; then
  ENABLE_DATADOG="true"
else
  ENABLE_DATADOG="false"
fi
```

INPUT_JSON: {"configKey": "config/...", "enable_datadog_metrics": "true"}
                                                                    ^^^^^^
                                                                    This should be "true" when you select "yes"


Step 2: Step Functions Execution (passes to first Lambda)
---------------------------------------------------------
The INPUT_JSON is sent to the ReadConfig Lambda function


Step 3: ReadConfig Lambda
--------------------------
FILE: iac/lambda/read-config/index.py
LINE: 59-65

Receives: event['enable_datadog_metrics'] = "true" or "false"

```python
enable_datadog_from_workflow = event.get('enable_datadog_metrics', '').lower() == 'true'
if enable_datadog_from_workflow:
    print("🐶 Datadog metrics enabled via workflow input (enable_datadog_metrics=true)")
```

If TRUE, sets on each test object:
LINE: 83-87
```python
test['enableDatadog'] = True
if 'datadogSite' not in test:
    test['datadogSite'] = 'datadoghq.com'
print(f"  ✅ Datadog enabled for test: {test['testId']}")
```

OUTPUTS: testSuite with each test having enableDatadog=True


Step 4: JMX Parser Lambda (passes through)
------------------------------------------
FILE: iac/lambda/jmx-parser/index.py
This Lambda doesn't modify the enableDatadog flag, just passes it through


Step 5: SubmitTasks Lambda
---------------------------
FILE: iac/lambda/submit-tasks/index.py  
LINE: 123-128

Checks each test object:
```python
if test.get('enableDatadog', False):
    environment.append({'name': 'ENABLE_DATADOG_METRICS', 'value': 'true'})
    dd_site = test.get('datadogSite', 'datadoghq.com')
    environment.append({'name': 'DD_SITE', 'value': dd_site})
    print(f"    📊 Datadog metrics enabled for container {container_idx} (site: {dd_site})")
```

ADDS TO ECS TASK: Environment variable ENABLE_DATADOG_METRICS="true"


Step 6: ECS Container Entrypoint
---------------------------------
FILE: docker/entrypoint.sh
LINE: 337-380

Receives environment variable: ENABLE_DATADOG_METRICS="true"

Checks:
```bash
if [ "${ENABLE_DATADOG_METRICS:-false}" = "true" ]; then
    echo "[DATADOG] Starting Metrics Forwarder"
    # ... starts forwarder
else
    echo "[DATADOG] Metrics disabled (ENABLE_DATADOG_METRICS=${ENABLE_DATADOG_METRICS:-false})"
fi
```

SHOULD PRINT: "[DATADOG] Starting Metrics Forwarder"


VERIFICATION CHECKLIST FOR YOU:
================================

□ 1. Check .github/workflows/run-test.yml lines 110-122
   - Verify the conversion logic exists
   - Verify INPUT_JSON uses ENABLE_DATADOG variable (not ENABLE_DATADOG_INPUT)

□ 2. Check iac/lambda/read-config/index.py line 59
   - Must be: == 'true' (NOT == 'yes')

□ 3. Check iac/lambda/read-config/index.py line 83
   - Must set: test['enableDatadog'] = True

□ 4. Check iac/lambda/submit-tasks/index.py line 123
   - Must check: test.get('enableDatadog', False)

□ 5. Check docker/entrypoint.sh line 338
   - Must check: [ "${ENABLE_DATADOG_METRICS:-false}" = "true" ]

□ 6. MOST IMPORTANT: Check if Lambda changes are deployed
   - Go to AWS Lambda Console
   - Find function: JMeterEcsStack-ReadConfigXXXXX
   - Check "Last modified" date - should be TODAY
   - View code and verify line 59 has == 'true'


COMMON ISSUES:
==============

Issue 1: Lambda not deployed
- Changes in GitHub but not in AWS
- Solution: Wait for GitHub Actions deployment or manually deploy

Issue 2: Using old container image  
- Docker image needs rebuild
- Solution: GitHub Actions should rebuild automatically

Issue 3: Cached Step Functions state
- Old execution using old Lambda
- Solution: Start a NEW test execution


DEBUGGING COMMANDS:
===================

1. Check Lambda function code in AWS:
   aws lambda get-function --function-name JMeterEcsStack-ReadConfig* --query 'Code.Location'

2. Check Lambda last modified:
   aws lambda get-function --function-name JMeterEcsStack-ReadConfig* --query 'Configuration.LastModified'

3. Check ECR image tags:
   aws ecr describe-images --repository-name jmeter-framework --query 'imageDetails[0].imagePushedAt'

4. Check Step Functions execution input:
   aws stepfunctions describe-execution --execution-arn <ARN> --query 'input'


EXPECTED LOG OUTPUT (when working):
====================================

ReadConfig Lambda CloudWatch:
🐶 Datadog metrics enabled via workflow input (enable_datadog_metrics=true)
  ✅ Datadog enabled for test: dcp-api-may-v2

SubmitTasks Lambda CloudWatch:
    📊 Datadog metrics enabled for container 0 (site: datadoghq.com)

ECS Container CloudWatch:
[DATADOG] Starting Metrics Forwarder
[DATADOG] Configuration:
  API Site: datadoghq.com
  Tags: test_id:...,run_id:...,container_id:...


CURRENT ISSUE:
==============
You're seeing: [DATADOG] Metrics disabled (ENABLE_DATADOG_METRICS=false)

This means EITHER:
A) Lambda changes not deployed yet (most likely)
B) Test object doesn't have enableDatadog=True
C) SubmitTasks not setting environment variable

Check Lambda deployment first!